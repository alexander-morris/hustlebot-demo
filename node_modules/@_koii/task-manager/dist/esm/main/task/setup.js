var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import { coreLogic } from "./coreLogic.js";
import { namespaceWrapper, app } from "@_koii/namespace-wrapper";
export function setupTask() {
    return __awaiter(this, void 0, void 0, function* () {
        if (app) {
            app.get("/taskState", (_req, res) => __awaiter(this, void 0, void 0, function* () {
                const state = yield namespaceWrapper.getTaskState({
                    is_submission_required: true,
                    is_distribution_required: true,
                    is_available_balances_required: true,
                    is_stake_list_required: true,
                });
                console.log("TASK STATE", state);
                res.json({ taskState: state });
            }));
        }
        try {
            yield namespaceWrapper.defaultTaskSetup();
        }
        catch (error) {
            console.error("SETUP ERROR:", error);
        }
        process.on("message", (m) => {
            console.log("CHILD got message:", m);
            if (!(m === null || m === void 0 ? void 0 : m.functionCall)) {
                return;
            }
            console.log(`${m.functionCall} called`);
            switch (m.functionCall) {
                case "submitPayload":
                    coreLogic.submitTask(m.roundNumber);
                    break;
                case "auditPayload":
                    coreLogic.auditTask(m.roundNumber);
                    break;
                case "executeTask":
                    coreLogic.task(m.roundNumber);
                    break;
                case "generateAndSubmitDistributionList":
                    coreLogic.selectAndGenerateDistributionList(m.roundNumber, m.isPreviousRoundFailed);
                    break;
                case "distributionListAudit":
                    coreLogic.auditDistribution(m.roundNumber);
                    break;
            }
        });
    });
}

import { getTaskManager } from "../taskManager.js";

class Task {
  /**
   * Executes your task, optionally storing the result.
   *
   */
  async executeTask(round: number): Promise<void> {
    const taskManager = await getTaskManager();
    await taskManager.executeTask(round);
  }
}

export const task = new Task();
